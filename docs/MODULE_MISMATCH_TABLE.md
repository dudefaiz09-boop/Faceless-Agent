# Module Mismatch Table

| Module        | Backend route          | Shared API method      | Web caller                | Mobile caller     | Role/tenant dependency               | Current issue                            | Fix applied                                      |
| ------------- | ---------------------- | ---------------------- | ------------------------- | ----------------- | ------------------------------------ | ---------------------------------------- | ------------------------------------------------ |
| announcements | `/api/announcements`   | `AnnouncementsService` | partial                   | partial           | auth + tenant + target roles/classes | raw web calls remain                     | service existed, error handling centralized      |
| attendance    | `/api/attendance`      | `AttendanceService`    | raw remains               | raw remains       | auth + tenant + class/student        | raw endpoints                            | service added                                    |
| assignments   | `/api/assignments`     | `AssignmentsService`   | typed                     | typed screen      | auth + tenant + class/student        | backend validation still partial         | existing service retained, client hardened       |
| chat/AI       | `/api/chat`, `/api/ai` | `ChatbotService`       | raw remains               | raw remains       | auth + tenant + AI availability      | route handlers still mixed               | client/error hardening                           |
| fees          | `/api/fees`            | `FeesService`          | typed                     | raw remains       | auth + tenant + fee permissions      | raw web calls, import idempotency absent | web switched to service, idempotency keys added  |
| library       | `/api/library`         | `LibraryService`       | raw remains               | raw remains       | auth + tenant + library permissions  | raw endpoints                            | service added                                    |
| notifications | `/api/notifications`   | `NotificationsService` | raw dropdown remains      | service available | auth + tenant + target user          | raw endpoints                            | service added                                    |
| performance   | `/api/performance`     | `PerformanceService`   | raw remains               | raw remains       | auth + tenant + class/student        | raw import                               | service added                                    |
| students      | `/api/students`        | `StudentsService`      | partial                   | service available | auth + tenant + class                | mixed routes                             | service existed                                  |
| teachers      | `/api/teachers`        | `TeachersService`      | raw remains               | none found        | auth + tenant + admin                | raw endpoints                            | service added                                    |
| users         | `/api/users`           | `UsersService`         | typed for critical writes | none found        | auth + tenant + admin                | demo tenant fallback, raw writes         | web switched for create/update/deactivate/import |
| roles         | `/api/roles`           | `RolesService`         | none found                | none found        | auth + tenant + admin                | no typed service                         | service added                                    |
| parent portal | composed routes        | `ParentPortalService`  | raw remains               | raw remains       | auth + tenant + linkedStudentIds     | raw composed fetches                     | service added                                    |
